name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: npm ci
      run: npm ci
    - name: build
      run: npm run release

  test:

    runs-on: ubuntu-latest

    steps:
      - name: npm ci
        run: npm ci
      - name: test
        run: npm run test
